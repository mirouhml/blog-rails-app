<% @users.each do |user| %>
  <section class="users">
    <div class="user">
      <img src=<%= user.photo %> alt="user_photo">
      <div class="user-info">
        <h2><%= user.name %></h2>
        <p>Number of posts: <%= user.PostsCounter %></p>
      </div>
    </div>
  </section>
<% end %>